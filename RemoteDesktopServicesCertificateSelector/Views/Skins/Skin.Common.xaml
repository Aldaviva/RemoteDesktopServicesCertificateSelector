<!-- ReSharper disable InconsistentNaming - many of these resource keys are autogenerated -->
<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:viewModels="clr-namespace:RemoteDesktopServicesCertificateSelector.ViewModels">

    <Thickness x:Key="cellPadding" Left="6" Top="4" Right="6" Bottom="4" />

    <SolidColorBrush x:Key="windowBackgroundBrush" Color="White" />
    <Style TargetType="Window" x:Key="windowStyle">
        <Setter Property="Background" Value="{StaticResource windowBackgroundBrush}" />
    </Style>

    <Style TargetType="DataGridCell" x:Key="cellStyle">
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Padding" Value="{DynamicResource cellPadding}" />
        <Setter Property="Foreground" Value="{DynamicResource textBrush}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="DataGridCell">
                    <!-- Get rid of black outline on clicked cell -->
                    <Border Padding="{TemplateBinding Padding}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}"
                            Background="{TemplateBinding Background}" SnapsToDevicePixels="True">
                        <ContentPresenter SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsSelected" Value="True">
                <Setter Property="Background" Value="{DynamicResource dataGridCellSelectedBackground}" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- Expired certificate date is red -->
    <Style TargetType="TextBlock" x:Key="expirationDateStyle">
        <Setter Property="Margin" Value="2 0" />
        <Style.Triggers>
            <!-- Mode must be OneTime or else you get Key Cannot Be Null crashes -->
            <DataTrigger Binding="{Binding Item.(viewModels:CertificateViewModel.isExpired), RelativeSource={RelativeSource AncestorType=DataGridRow}, Mode=OneTime}"
                         Value="true">
                <Setter Property="Foreground" Value="{DynamicResource invalidBrush}" />
            </DataTrigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="iconButtonStyleCommon" TargetType="Button">
        <Setter Property="Padding" Value="5,1,6,1" />
        <Setter Property="HorizontalContentAlignment" Value="Left" />
        <Setter Property="Height" Value="24" />
    </Style>

</ResourceDictionary>